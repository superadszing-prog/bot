/**
 * Instagram-specific entry point. Reels/feed posts use <video> elements
 * within dynamically loaded containers, handled by the shared detector.
 */
(function () {
  new self.AIBotVideoBotController('instagram');
})();
